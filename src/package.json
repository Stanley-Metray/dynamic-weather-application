{
  "dependencies": {
    "express": "^4.17.2",
    "pug": "^3.0.2",
    "requests": "^0.3.0",
    "serve-favicon": "^2.5.0",
    
    "scripts" : {
      "test": "echo \"Error: no test specified\" && exit 1",
      "start" : "node src/index.mjs"
    }
  }
}
